<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardMapper">

	<resultMap type="board" id="boardMap">
		<result property="id" column="id" jdbcType="NUMERIC" />
		<result property="index" column="index" jdbcType="NUMERIC" />
		<result property="name" column="name" jdbcType="VARCHAR" />
		<result property="description" column="description"
			jdbcType="VARCHAR" />
		<result property="isActive" column="is_active" jdbcType="CHAR" />
	</resultMap>

	<insert id="addBoard" parameterType="Board">
		<selectKey resultType="int" keyProperty="id" order="BEFORE">
			select nextval('board_id_seq')
		</selectKey>
		INSERT INTO
			board
			(id, index, name, description, is_active)
		VALUES
			(#{id}, #{index}, #{name}, #{description}, 'A')
	</insert>

	<select id="getBoardList" resultMap="boardMap">
		SELECT
			id, index, name, description, is_active
		FROM
			board
		ORDER BY
			index ASC
	</select>
	
</mapper>